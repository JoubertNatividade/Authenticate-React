import { createGlobalStyle } from "styled-components";



export const GlobalStyles = createGlobalStyle` 
    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        /* background-image: linear-gradient(320deg, #111, #f1f1f1);        */
        background: #f4f4f4;
        font-family: 'Roboto',sans-serif;
    }

   
`